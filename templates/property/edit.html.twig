{% extends 'base.html.twig' %}
{% block title %}Edit Property{% endblock %}
{% block body %}
    <div class="container-fluid">
        <div class="row my-4">
            <div class="col-6">
                <h4>Edit Property</h4>
            </div>
            <div class="col-6 text-end">
                <a href="{{ path('app_property_index') }}" class="btn btn-primary">Back to list</a>
            </div>
        </div>
        <div class="row my-4">
            <div class="col-12">
                {{ include('property/_form.html.twig', {'button_label': 'Update'}) }}
            </div>
        </div>
    </div>
{% endblock %}

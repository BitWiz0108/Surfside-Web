<form method="post" action="{{ path('app_linen_delete', {'id': linen.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ linen.id) }}">
    <button class="btn btn-danger btn-sm">Delete</button>
</form>

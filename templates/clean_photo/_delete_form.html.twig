<form method="post" action="{{ path('app_clean_photo_delete', {'id': clean_photo.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ clean_photo.id) }}">
    <button class="btn btn-outline-danger btn-sm">Delete</button>
</form>

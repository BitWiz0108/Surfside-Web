{% extends 'base.html.twig' %}
{% block title %}Edit Clean{% endblock %}
{% block body %}
    <div class="container-fluid">
        <div class="row my-4">
            <div class="col-6">
                <h4>Edit Cleaning Appointment</h4>
            </div>
            <div class="col-6 text-end">
                <a href="{{ path('app_clean_index') }}" class="btn btn-secondary btn-sm">Back To List</a>
            </div>
        </div>
        <div class="row my-4">
            <div class="col-12">
                {{ include('clean/_form.html.twig', {'button_label': 'Update'}) }}
            </div>
        </div>
    </div>
{% endblock %}

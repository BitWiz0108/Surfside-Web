<form method="post" action="{{ path('app_clean_supply_delete', {'id': clean_supply.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ clean_supply.id) }}">
    <button class="btn">Delete</button>
</form>
